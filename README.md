# Features:
1. Django application which can be cloned, make migration and just like that run the django application using the famouse runserver command.
2. It uses sqlite as db store.
3. Create your own virtual env with python and run requirements.txt and run the server.
4. It has features like user throttling, limiting external API call, api-key authentication, cache and search support.
